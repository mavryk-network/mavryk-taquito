name: Bundle Webpack

on:
  push:
    branches:
      - master

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}-${{ github.ref != 'refs/heads/master' || github.run_number }}
  cancel-in-progress: ${{ startsWith(github.ref, 'refs/pull/') }}

jobs:
  bundle-webpack:
    runs-on: ubuntu-latest
    steps:
    -
      name: Checkout repository
      uses: actions/checkout@v2
    -
      uses: actions/setup-node@v2
      with:
        node-version: lts/hydrogen
    - run: npm ci
    - run: npm run build
    - run: npm -w packages/webmavryk-local-forging run build-webpack
    -
      uses: actions/upload-artifact@v3
      with:
        name: webmavryk-local-forging-vanilla
        path: |
          packages/webmavryk-local-forging/dist/webmavryk_local_forging.js
          packages/webmavryk-local-forging/dist/webmavryk_local_forging.js.map

    - run: npm -w packages/webmavryk-beacon-wallet run build-webpack
    -
      uses: actions/upload-artifact@v3
      with:
        name: webmavryk-beacon-wallet-vanilla
        path: |
          packages/webmavryk-beacon-wallet/dist/webmavryk_beacon_wallet.js
          packages/webmavryk-beacon-wallet/dist/webmavryk_beacon_wallet.js.map
